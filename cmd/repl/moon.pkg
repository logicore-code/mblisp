// REPL package — see main.mbt.
//
// `pkgtype(kind: "executable")` makes `moon run cmd/repl` launch
// the interactive interpreter loop.

import {
  "logicore-code/mblisp",
}

pkgtype(kind: "executable")